// 新增查詢功能的 JavaScript 函數
function queryCustomer() {
  // 簡化範例：直接將結果顯示在 #query-result 中
  document.getElementById("query-result").innerHTML = "Query Customer Result";
}

function queryOrder() {
  // 簡化範例：直接將結果顯示在 #query-result 中
  document.getElementById("query-result").innerHTML = "Query Order Result";
}

function queryProduct() {
  // 簡化範例：直接將結果顯示在 #query-result 中
  document.getElementById("query-result").innerHTML = "Query Product Result";
}
